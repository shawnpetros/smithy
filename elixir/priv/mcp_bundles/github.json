{
  "_comment": "GitHub MCP server. Anthropic ships @modelcontextprotocol/server-github. Requires a GITHUB_PERSONAL_ACCESS_TOKEN env var with repo+read scopes for the repos the agent will touch.",
  "github": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github"],
    "env": {
      "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
    }
  }
}
